
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Agregar precios</h1>
    <form action="/add" method="POST">
        <input type="text" step="0.01" name="valor" placeholder="Ingresa el valor" required>
        <button type="submit">Agregar</button>
    </form>
    <h2>Precios agregados</h2>
    <ul>
        <% if (precios && precios.length > 0) { %>
            <% precios.forEach(precio => { %>
                <li>Precio: <%= precio.precio %></li>
            <% }); %>
        <% } else { %>
            <li>No hay precios agregados aún.</li>
        <% } %>
    </ul>
    <h2>Total <%=total%></h2>
</body>
</html>